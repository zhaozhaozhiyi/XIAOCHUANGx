# 小窗 — 功能清单（菜单结构）

> 信息架构原则：**按用户可感知的能力划分一级模块**。  
> 能力编号（F-QA / F-ARA / F-WR / F-PPT / **F-RT** / **F-SET** / **F-AUTH**）保留于「能力映射」列，便于研发追溯。  
> **MVP（v3.5，[PRD](./PRD-小窗.md) §1.2、§10.2、§12.5.9）：** 仅深度验收 **对话** + **Electron 桌面壳** + Companion/CLI + 登录 + 全局设置子集；纪要/知识库/写作/PPT/翻译 **仅占位**。收口见 [mvp-closure-checklist.md](./web/docs/mvp-closure-checklist.md)。  
> **横切：** 账号 §6.12；智能体 §6.10（F-RT-003/008/009-A 等）；数据分工 §8.6。切换本地项目 = **新建对话会话**。  
> **技术选型：** [技术方案.md](./技术方案.md)。**工程：** `web/`、`companion/`、`apps/desktop/`、`api/`、`packages/*`。

---

## 导航总览

```
小窗
├── 对话
├── 会议纪要
├── 知识库
├── 写作
├── PPT
└── 翻译

（登录：/login，未登录不可进业务页）
（全局设置：侧栏底部「用户区」弹出菜单，不占左侧业务导航）
```

---

## 一级 / 二级菜单

| 一级菜单 | 二级菜单 | 说明 | 能力映射 |
|----------|----------|------|----------|
| **对话** | 新对话 | 发起智能问答，支持附件；回复内项目文件可点击打开工作区（F-QA-010） | F-QA-001～005、007、008、010 |
| | 历史会话 | 侧栏/全屏列表查看、继续、删除；每条展示执行/未读/待确认状态（F-QA-008） | F-QA-008 |
| **会议纪要** ⏸️ V1.1+ 推迟 | 新建纪要 | 上传音视频，生成纪要（2026-06-06 推迟到 V1.1+） | F-ARA-003 |
| | 纪要历史 | 历史纪要列表与导出 | F-ARA-003 |
| **知识库** ⏸️ V1.1+ 推迟 | 我的文档 | 上传、分类、标签、搜索、管理（2026-06-06 推迟到 V1.1+） | F-ARA-004 |
| | 知识库问答 | 基于库内文档问答 | F-ARA-004 |
| | 多信源分析 | 库内文档参与异同分析（可链至对话） | F-ARA-004、F-QA-004 |
| **写作** | 写作 | 对话式写作；默认 `skill-writing-general`，对话内可切换 `skill-wr-policy/topic/industry/macro/sector-data` 等扩展 Skill；五类模板由 Composer 下拉或自然语言触发；产出 `.md` 落工作区即「我的文稿」（[writing-module-prd.v2](./web/docs/writing-module-prd.v2.md) 2026-05-31 决策） | F-WR-001 |
| **PPT** | PPT | 对话式生成演示文稿；默认 `skill-ppt-pitch-deck`，对话内可切换其他 PPT Skill；从文稿/路演模板由自然语言或切换 Skill 触发；产出 `.pptx`/`.html` 落工作区即「我的 PPT」（[ppt-module-prd.v2](./web/docs/ppt-module-prd.v2.md) 2026-05-31 决策） | F-PPT-001 |
| **翻译** | 翻译 | 对话式翻译；默认 `skill-tr-text`，对话内可切换 `skill-tr-doc/polish`；文本/文档/润色由 Composer 下拉或自然语言触发；产出对话回复 + `.md` 落工作区即「翻译历史」（[translate-module-prd](./web/docs/translate-module-prd.md) 2026-06-06 决策） | F-ARA-002 |
| **全局设置**（用户区弹出） | 智能体与模型 | 预置 CLI 状态；默认 Agent/模型；重新检测 | F-SET-001 |
| | 研究与对话默认 | 默认问答模式等 | F-SET-002（V1.1） |
| | 数据与图表 | 变频/导出默认 | F-SET-003（V1.1） |
| | 工作区 | 默认展开、宽度、终端页签 | F-SET-004（V1.1） |
| | 知识库 | 容量用量、入库提示 | F-SET-005（V1.1） |
| | 账号与权限 | 脱敏手机号、模块与数据权限（只读） | F-SET-006 |
| **账号与登录** | 登录页 | 手机号 + 验证码；登录即注册；退出 | F-AUTH-001～007 |
| | 关于与帮助 | 版本、诊断、反馈 | F-SET-007 |
| | 模型 API / 功能与审计 | 模型 API 接入、模块开关、审计（管理员增强） | F-SET-008 |
| **智能体与工作区**（横切） | 研究项目与会话 | 始终 `projectId`；A 默认 cloud / B 未选课题 → §5.3.2.1a 平台默认工作区；固定绑定 | F-RT-007 |
| | 本地 Companion | 安装、连接；`local_bound`（用户课题 / XIAOCHUANG 平台默认） | F-RT-007b |
| | 文件夹导入 | Web 手填路径 / 桌面系统选目录 → `import-folder`；**不复制**目录 | F-RT-007c |
| | 切换项目 | 「进入项目工作」选本地项目 → **新建对话会话** | F-RT-007 |
| | 工作区 | 模式 B 本地树 / 模式 A 云端；预览、导出、入库 | F-RT-004 |
| | 执行可视 | 流式输出、工具调用、计划卡片、取消 | F-RT-005 |
| | 对话编排 | 混合编排 · 基座 + Catalog 摘要；Agent 自选扩展 Skill | F-RT-008 |
| | 超长会话 | MVP：自动压缩后继续；V1.1+BYOK：LLM Handoff 摘要 | F-RT-009 |
| | 终端面板 | 用户 / Agent 只读终端分组 | F-RT-006 |

---

## 各一级模块功能明细

### 1. 对话

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 问答模式 | 页内模式 | 快速、深度（子策略由 Agent 在 `skill-qa-deep` 内决策，PRD v3.2） |
| 数据源 | 能力 | 调用数据源（行情、指标等）、指标、资讯、公告、研报 |
| 可交互图表 | 能力 | 表格/图表切换；编辑、排序、环比、变频 |
| 图例与导出 | 能力 | 图例自定义；复制；导出 Excel/CSV/PNG |
| 多信源分析 | 能力 | 分信源总结、综合总结、异同分析、信源推荐 |
| 可溯源 | 能力 | 结论/数据/引用跳转原文或终端详情 |
| 附件上传 | 能力 | 对话中上传补充材料 |

### 2. 会议纪要

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 新建纪要 | 二级 | 上传 mp3/mp4/wav/m4a |
| 纪要历史 | 二级 | 历史列表与导出 |
| 语音转写 | 能力 | 中文语音转文字 |
| 多发言人 | 能力 | 说话人分离与重命名 |
| 会议概要 | 能力 | 200–500 字摘要 |
| 要点大纲 | 能力 | 层级标题 + 要点 |
| QA 环节 | 能力 | 自动识别问答对 |
| 存入知识库 | 能力 | 纪要结果可沉淀至知识库 |

### 3. 知识库

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 我的文档 | 二级 | 上传、分类、标签、搜索、删除 |
| 知识库问答 | 二级 | 针对库内文档问答，片段可定位 |
| 多信源分析 | 二级 | 与对话多信源结构打通 |
| 格式支持 | 能力 | PDF、Word、Txt、Markdown |
| 容量配额 | 能力 | 按用户/企业空间隔离与限额 |

### 4. 写作（v2 设计：对话式 + Skill 切换；2026-05-31）

> 详见 [`web/docs/writing-module-prd.v2.md`](./web/docs/writing-module-prd.v2.md)。  
> v2 决策核心：**写作 = 对话 + 写作 Skill + MD 交付 + DOCX 导出**；工作区文件即文稿。  
> v1 PRD（含「五类模板 / 我的文稿」7 二级菜单 + 多步骤向导）已于 2026-06-06 废止。

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 写作（一项二级） | 二级 | `/writing/new`，加载默认 `skill-writing-general` 进入对话；五类模板（政策/专题/行业/宏观/行业数据）由 Composer 下拉或对话内切换 `skill-wr-*` 触发，不另开页面 |
| 对话内 Skill 切换 | 能力 | Composer 下拉显示 `skill-writing-general` + `skill-writing-official-doc` + `skill-writing-meeting-minutes` + 5 个 `skill-wr-*` |
| 大纲编辑 | 能力 | 在对话中由 Agent 用工具调用读写 outline 文件，**非独立 UI** |
| 嵌入图表 | 能力 | 复用对话模块表格/图表能力 |
| 文稿导出 | 能力 | `/api/writing/export-docx`（Pandoc）；PDF V1.1+ |
| 加入知识库 | 能力 | 成稿可沉淀为库内文档 |
| 我的文稿列表 | ❌ 已废止 | v2：工作区文件即文稿（PRD v2 §1.3）；如需复用历史走对话历史侧栏 |

### 5. PPT（v2 设计：对话式 + Skill 切换；2026-05-31）

> 详见 [`web/docs/ppt-module-prd.v2.md`](./web/docs/ppt-module-prd.v2.md)。  
> v2 决策核心：**PPT = 对话 + PPT Skill + 交付物 + 导出**；工作区文件即 PPT。  
> v1 PRD（含「新建 / 从文稿 / 路演模板 / 我的 PPT」四二级菜单）已于 2026-06-06 废止。

| 功能项 | 类型 | 说明 |
|--------|------|------|
| PPT（一项二级） | 二级 | `/ppt/new`，加载默认 `skill-ppt-pitch-deck` 进入对话；其他模板（周报、季报、瑞士、归藏…）由对话内切换 Skill 触发，不另开页面 |
| 对话内 Skill 切换 | 能力 | Composer 下拉显示所有 PPT Skill（`PPT_SKILL_CATALOG` 17 个） |
| 大纲编辑 | 能力 | 在对话中由 Agent 用工具调用读写 outline 文件，**非独立 UI** |
| 嵌入图表 | 能力 | 复用对话/写作中的数据图（导出为静态图） |
| 交付物预览 | 能力 | DeliverablesCard 列出 .pptx/.html 文件，点击在工作区查看 |
| 导出 PPTX | 能力 | `/api/ppt/download`；可选 PDF |
| 加入知识库 | 能力 | 导出 PDF 或讲义文稿沉淀 |
| 我的 PPT 列表 | ❌ 已废止 | v2：工作区文件即 PPT（PRD v2 §1.3）；如需复用历史走对话历史侧栏 |

### 6. 翻译

> v2（2026-06-06）：参考 [translate-module-prd.md](./web/docs/translate-module-prd.md)。原 PRD-小窗 §6.3 描述的「文档翻译 / 文本翻译 / 翻译历史」3 个二级菜单已废止；现行只保留「翻译」一项，文档/文本/润色由对话内切换 Skill 触发。

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 翻译（一项二级） | 二级 | `/translate/new`，加载默认 `skill-tr-text` 进入对话；文档翻译 / 译文润色由对话内切换 `skill-tr-doc / skill-tr-polish` 触发，不另开页面 |
| 对话内 Skill 切换 | 能力 | Composer 下拉 3 个翻译 Skill（text / doc / polish），templateId 持久化到 `localStorage` |
| 多备选译文 | 能力 | Skill 层强制 ≥ 2 个译文备选 + 说明段，避免机翻直白 |
| 层级保留 | 能力 | 文档翻译保留 H1/H2/H3、表格、列表、引用、代码块；术语对照表附录 |
| 对照模式 | 能力 | 文档翻译用户要求时输出原文-译文表格对照 |
| 语言检测 | 能力 | Skill 自行判断并告知用户，可纠正；中英互译为默认范围 |
| 交付物预览 | 能力 | 文档翻译 `.md` 通过 DeliverablesCard 列出，复用工作区文件通道 |
| 翻译历史列表 | ❌ 已废止 | v2：对话历史侧栏 + 工作区文件即历史 |

---

## 对话 — 模式说明（页内切换，非侧栏二级）

「**快速 / 深度**」在对话页**模式选择器**中切换（页面上不区分深度思考与深度研究）；侧栏二级仅保留「新对话」「历史会话」。

**混合编排（PRD v3.5 F-RT-008 · 已实现）：** 平台 Push 基座 Skill + Skill Catalog 摘要 + 编排指引；PPT/写作等扩展 Skill 与 CLI 工具由 Agent **按需**选用，无每轮强制 Router。见 [web/docs/chat-core-architecture.md](./web/docs/chat-core-architecture.md)、PRD §12.5.9。

---

## 写作 — 生成流程（v2：对话驱动，非分步表单）

> v2 决策（2026-05-31）：废止"参数设置 → 写作方向 → 大纲确认 → 撰写"分步向导。  
> 写作流程 **= 对话流程**：用户在对话内描述需求，Agent 按所选 Skill（默认 `skill-writing-general` 或对话内切换的 `skill-wr-*`）引导是否需要先确认大纲。  
> 大纲确认与多步骤是 **Skill 内的可选行为**，不是 UI 表单。

| 写作场景 | 默认 Skill | 备注 |
|---------|-----------|-----|
| 通用写作 | `skill-writing-general` | 进入 `/writing/new` 默认加载 |
| 公文 | `skill-writing-official-doc` | Composer 下拉切换 |
| 会议纪要写作（非录音转写） | `skill-writing-meeting-minutes` | 同上 |
| 政策解读 | `skill-wr-policy` | 同上；2026-06-06 占位 |
| 专题研究 | `skill-wr-topic` | 同上 |
| 行业研究 | `skill-wr-industry` | 同上 |
| 宏观数据解读 | `skill-wr-macro` | 同上 |
| 行业数据解读 | `skill-wr-sector-data` | 同上 |

~~v1 多步骤 / 快速两档分步流程~~ → 改为 Skill 内引导，**不在 UI 层暴露**（PRD v2 §1.3）。

---

## 账号与登录（横切）

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 登录页 | 页面 | `/login`；未登录跳转 |
| 手机号 + 验证码 | 能力 | 登录即注册；6 位码；60s 重发间隔 |
| 用户协议 | 合规 | 首次登录勾选 |
| 会话 | 能力 | 登录态校验；过期重新登录 |
| 退出登录 | 能力 | 清会话 → `/login` |
| 微信 / 外部 SSO 绑定 | — | **MVP 不做** |

---

## 全局设置（用户区弹出菜单，横切）

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 入口 | 交互 | 侧栏底部用户区点击 → L1 弹出菜单 → L2 设置面板 |
| 智能体与模型 | 设置页 | F-SET-001；多 CLI 卡片 + 默认 Agent/模型 |
| 研究与对话默认 | 设置页 | F-SET-002（V1.1） |
| 数据与图表默认 | 设置页 | F-SET-003（V1.1） |
| 工作区偏好 | 设置页 | F-SET-004（V1.1） |
| 知识库用量 | 设置页 | F-SET-005（V1.1） |
| 账号与权限 | 设置页只读 | F-SET-006；脱敏手机号、模块与数据权限摘要 |
| 关于与帮助 | 设置页 | F-SET-007；版本与诊断 |
| 功能与审计 | 设置页管理员 | F-SET-008（V1.1）；BYOK 见 F-SET-001 |

**MVP 弹出菜单（研究员）：** 智能体与模型 · 账号与权限 · 关于与帮助

---

## 智能体与工作区（横切）

| 功能项 | 类型 | 说明 |
|--------|------|------|
| 模式 B 本地协作 | 主路径 | Companion + `projectId`（`local_bound`：用户课题或 XIAOCHUANG 平台默认）+ 本机 CLI |
| 平台默认工作区 | 模式 B 未选课题 | 新建任务 → `{文稿}/XIAOCHUANG/{模块}/{YYYY-MM-DD}/{标题}/` + `ensure-default-task-project`（PRD §5.3.2.1a） |
| `sandbox` | 内部/迁移 | 非用户可见默认；量产禁止回退 `sandbox-default` |
| 云端项目 | 模式 A 默认 | 无 Companion → 新建对话自动 cloud + OSS |
| 本地绑定项目 | 能力 | **文件夹导入** → `baseDir` 原目录读写；须**新建对话会话**绑定（F-RT-007c） |
| 添加新项目（Web） | 交互 | 路径输入 `~/Projects/...`；禁止 Directory Picker 主路径 |
| 添加新项目（桌面） | 交互 | Electron 系统选目录；Web 仅见 `projectId`（V1.1） |
| 会话固定 projectId | 规则 | 创建时绑定不可改；换文件夹=新建会话；分支继承 projectId |
| 模式 A 云端子集 | MVP | 见 PRD §10.2（对话 + 文件树；写作/PPT 可收缩） |
| 平台 CLI 适配集 | 平台 | 多 CLI / 多模型适配；IT 桌面镜像或 Companion 捆绑 |
| 企业扩展 CLI | 策略 | 企业扩展表登记；Web 无任意 exe 路径 |
| 默认 Agent 选择 | 设置 F-SET-001 | 列表内切换；对话区「进入项目工作」 |
| Companion / CLI 状态 | 设置 F-SET-001 | 连接、可用 / 未安装 / 需登录 / 版本过低 |
| 模型 API 接入 | 设置 F-SET-001/008 | MVP：OpenAI 兼容 + Anthropic；与 CLI 共用统一执行主干 |
| 模块注册表 | 平台 | 六个 `moduleId` → 领域服务、绑定键、流程 Skill、模板资产、是否 Agent 主路径（PRD §6.10.1a） |
| 流程 Skill | 平台 | 按 `mode` / `templateId` / `task` 绑定；非「一级菜单 = 一个 Skill」 |
| 横切规范 Skill | 平台 | `skill-platform-research-norms`，每次 Agent 任务叠加 |
| 模板资产包 | 平台 | 写作 `assets/template.*`、PPT/纪要 `tpl-*`；由流程 Skill 引用，非独立 Agent 入口 |
| 工作区文件 | 横切 UI | 当前 session 的 projectId 根；预览、下载、导出 |
| 流式与取消 | 横切 UI | 工具链、Todo、阶段进度、中断保留 partial |
| 桌面壳 | 交付 | **MVP**：Electron 加载 `web/` + 系统选目录；**V1.1**：HMAC、托盘、Companion 捆绑、自动更新 |

---

## 模块注册表（研发落地）

> 与 PRD §6.10.1a 一致；Web 侧建议以 `lib/module-registry.ts`（或等价配置）实现，运行时与 F-RT-003 共用。

| `moduleId` | 一级菜单 | 领域服务 | 绑定键 → 流程 Skill（MVP） | 模板资产 | Agent | 工作区 | `workspaceSegment` |
|------------|----------|----------|---------------------------|----------|-------|--------|-------------------|
| `chat` | 对话 | 数据源、图表 | `fast`→`skill-qa-fast`；`deep`→`skill-qa-deep`（`research` API 别名） | — | ✅ | ✅ | `会话` |
| `meeting` | 会议纪要 | ASR | `summary` + 可选 `templateId`→`skill-mm-*`（缺省 `skill-mm-summary`） | `tpl-mm-*` 可选 | ✅ | ✅ | `会议` |
| `knowledge` | 知识库 | 存储、检索、RAG | `kb-qa`→`skill-kb-qa`（V1.1） | — | 子场景 | ❌ | — |
| `writing` | 写作 | 数据源、图表 | `{templateId}`→`skill-wr-{id}` | per 模板 | ✅ | ✅ | `写作` |
| `ppt` | PPT | 幻灯片引擎 | `deck`→`skill-ppt-deck` | `tpl-ppt-*` | ✅ | ✅ | `PPT` |
| `translate` | 翻译 | 翻译引擎 | MVP 无 | — | ❌ | ✅（落盘/导出时） | `翻译` |

**每次 Agent 任务额外注入：** `skill-platform-research-norms`。

---

## 版本规划（按一级模块）

> **2026-06-06 产品决策：本地优先 + 桌面端 + 本机 Agent CLI**。  
> **Web 端 + Nest 多用户后台整体推到 V2.0**：原因是产品形态是"本机单用户 + Companion + agent CLI"，Nest/Postgres 多租户、跨设备会话同步、中央化审计这些能力在 V1.1 阶段没有实际承载需求；Web 浏览器版作为"无桌面壳降级"路径继续保留但不投入新功能。

| 版本 | 范围 |
|------|------|
| **MVP（v3.0）✅ 已收口（2026-06-06）** | 账号与登录；**对话**（F-QA-001/007/008/009，F-QA-010 P1）；**桌面壳**（`apps/desktop` + `pickAndImportFolder`）；智能体 F-RT-001～005、007/007b/007c；全局设置 MVP；其它一级模块**仅占位** |
| **V1.1（聚焦：桌面端能力 + 对话延展）** | **桌面壳 V1.1 套件**（HMAC + 托盘 + Companion 捆绑安装 + 自动更新 + 系统通知）、**写作 / PPT 收口**（v2 设计已落地，做最后 polish）、**翻译模块从 0**（导航 + UI + skill-tr-*）、**F-QA-002～005**（数据源/图表/多信源/溯源）、**F-RT-002 BYOK 增强**、**F-RT-009-B LLM Handoff**、**F-RT-006 终端面板真 PTY** |
| **V1.1+（推迟）** | **会议纪要**（ASR + 说话人 + 摘要，2026-06-06 推迟）、**知识库**（PDF/Word 解析 + RAG + 配额，2026-06-06 推迟） |
| **V2.0（云端 + 企业部署）** | **Nest `api/` 全量启用 + Postgres**（多用户 / 跨设备会话 / 审计）、**模式 A 云端工作区 + OSS**、产出同步云端、知识库企业共享、开放 API、Web 浏览器版作为多人协作主入口 |

---

## 统计

| 一级模块 | 二级菜单数 | 状态 |
|----------|------------|------|
| 对话 | 2（+ 3 页内模式） | 已定 |
| 会议纪要 | 2 | 已定 ⏸️ V1.1+ 推迟（2026-06-06） |
| 知识库 | 3 | 已定 ⏸️ V1.1+ 推迟（2026-06-06） |
| 写作 | 1 | 已定（v2 简化；2026-05-31） |
| PPT | 1 | 已定（v2 简化；2026-05-31） |
| 翻译 | 1 | 已定（v2 简化；2026-06-06） |
| 全局设置（用户区） | 3 项 MVP / 8 项完整 | 已定 |
| 智能体与工作区（横切） | — | 已定 |
| **桌面壳**（交付形态） | — | MVP 验收 |
| **合计** | **6 个一级业务模块（MVP 仅对话深度）+ 用户区全局设置 + 横切** | — |
