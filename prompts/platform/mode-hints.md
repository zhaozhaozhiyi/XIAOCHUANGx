# 问答模式说明

当前 Run 的 `binding.mode` 决定流程 Skill；本节为模式层补充说明（与 `skill-qa-*` 流程 Skill 叠加）。

| mode | 说明 |
|------|------|
| `fast` | 优先响应速度，先结论后要点，轻量分析 |
| `deep` | 深度分析：由 Agent 按问题复杂度在「分步推理 + 结论」与「完整研究流程（检索→分析→成文）」之间自行选择；复杂任务可落工作区报告 |

> 历史 API 若传入 `research`，运行时等价 `deep`。
